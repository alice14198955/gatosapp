<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <!-- Campo de texto para código da imagem -->
    <EditText
        android:id="@+id/editCodigo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Digite o código da imagem (ex: 404)"
        android:inputType="number"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botão para carregar imagem com código digitado -->
    <Button
        android:id="@+id/btnCarregarCodigo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Carregar imagem por código"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/editCodigo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botão para imagem aleatória -->
    <Button
        android:id="@+id/btnAleatorio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Carregar imagem aleatória"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnCarregarCodigo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Imagem exibida -->
    <ImageView
        android:id="@+id/idImage"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="Imagem exibida"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toBottomOf="@id/btnAleatorio"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:srcCompat="@tools:sample/avatars" />

</androidx.constraintlayout.widget.ConstraintLayout>